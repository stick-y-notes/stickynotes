{
  "name": "stickynotes",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "clean": "rm -rf dist && rm -rf .parcel-cache",
    "web4_testnet": "web4-deploy dist stickynotes.testnet --nearfs --yes",
    "web4_mainnet": "web4-deploy dist stickynotes.near --nearfs"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel": "^2.14.0"
  }
}
